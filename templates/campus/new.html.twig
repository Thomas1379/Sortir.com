{% extends 'base.html.twig' %}

{% block title %}New Campus{% endblock %}
{% block body %}

    <div class="new_Campus">
    <h1>Créer un nouveau Campus</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ include('campus/_form.html.twig') }}

    <a href="{{ path('app_campus_index') }}">back to list</a>
        <button class="btn" name=submit value="ajouter">Ajouter</button>
        <button class="btn" name=submit value="modifier">Modifier</button>
        <button class="btn" name=submit value="supprimer">Supprimer</button>

</div>
{% endblock %}
